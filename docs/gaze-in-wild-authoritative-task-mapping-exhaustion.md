# Gaze-in-the-Wild authoritative task-mapping search exhaustion

## Status

The authoritative numeric `TrIdx → task` mapping remains **unresolved**.

This checkpoint freezes a scoped search-exhaustion result. It means the enumerated
reviewed first-party/public avenues have been exhausted without recovering an explicit
numeric lookup. It does **not** mean that no authoritative mapping exists anywhere.

Frozen evidence fingerprint:

`c0e8e0da36d329d6445dace1a56646b89c9df5f4f406761311d588eddaf36aa7`

## What was exhausted

The reviewed scope now includes:

- the pinned `RSKothari/Gaze-in-Wild` repository across all 56 reachable commits,
  including the reviewed AppDesigner container;
- publication and supplementary task identities for the four named activities;
- public Figshare `ProcessData` and `LabelData` metadata;
- the PerForm-Lab-RIT institutional GIW extraction path;
- the **complete Git history of `ExtractedGIW.py`**, which contains exactly one
  historical commit for that file;
- targeted author-owned repository searches; and
- targeted public searches for explicit `PrIdx` / `TrIdx` task bindings.

No reviewed authoritative source explicitly enumerated the complete numeric mapping.

## What the PerForm-Lab source does establish

The pinned institutional extraction script is useful structural corroboration. It
contains the task directories:

1. `Indoor_Walk`
2. `Ball_Catch`
3. `Visual_Search`
4. `Tea_Making`

and reads `PrIdx` and `TrIdx` from `ProcessData` before addressing the raw GIW tree.

That co-occurrence is **not** a numeric lookup. Directory-list order is not evidence
that task 1 is `TrIdx 1`, task 2 is `TrIdx 2`, and so on. The complete history of the
reviewed extraction file contains no older mapping table to recover.

## Secondary corroboration remains secondary

The pinned ACE-DNV evidence corroborates the following usage in that secondary
methodological study:

| Trial index | Secondary task label | Status |
| --- | --- | --- |
| 1 | `Indoor_Walk` | secondary corroboration only |
| 2 | `Ball_Catch` | secondary corroboration only |
| 3 | `Visual_Search` | secondary corroboration only |
| 4 | — | unresolved |

ACE-DNV is not promoted to first-party authority. In particular, `TrIdx 4` is **not**
assigned to `Tea_Making` by elimination.

## Fail-closed boundary

This checkpoint does not authorize or create:

- an authoritative or complete numeric task mapping;
- `TrIdx 4 → Tea_Making`;
- task-stratified GIW validation or naturalistic-task sensitivity claims;
- new empirical performance results;
- cross-dataset validation;
- native-60-Hz or GP3 validity;
- acquisition-hardware cadence verification;
- quarantine exit; or
- a raw-data retention claim.

The existing exact participant-disjoint GIW benchmark therefore remains
**task-agnostic derived-60-Hz validation**.

## What can resolve the gate

The gate should move only if a newly recovered authoritative artifact or explicit
author/distributor clarification directly binds numeric `TrIdx` values—including
`TrIdx 4`—to publication task names. Publication ordering, task-directory ordering,
and elimination are intentionally insufficient.
